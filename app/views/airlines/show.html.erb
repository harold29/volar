<p style="color: green"><%= notice %></p>

<%= render @airline %>

<div>
  <%= link_to "Edit this airline", edit_airline_path(@airline) %> |
  <%= link_to "Back to airlines", airlines_path %>

  <%= button_to "Destroy this airline", @airline, method: :delete %>
</div>
